'use client';

import Image from 'next/image';
import { useCallback, useEffect, useRef, useState } from 'react';
import { ImagePlus, Loader2, MessageCircle, RefreshCw, Send, Sparkles, X, Heart, Flag } from 'lucide-react';
import { useForm } from 'react-hook-form';
import { z } from 'zod';
import { zodResolver } from '@hookform/resolvers/zod';
import { communitiesApi } from '@/features/communities/api';
import { uploadApi } from '@/features/upload/api';
import type { CommunityComment, CommunityPost, CommunityReactionType } from '@/types/community-social';
import { getErrorMessage } from '@/utils/error';
import { cn } from '@/utils/cn';
import toast from 'react-hot-toast';
import ClubChatLauncher from './ClubChatLauncher';

const postSchema = z.object({
  content: z.string().trim().max(5000, 'Bài viết tối đa 5000 ký tự.').optional(),
  topics: z.string().trim().max(200, 'Chủ đề quá dài.').optional(),
});
type PostFormValues = z.infer<typeof postSchema>;

function Avatar({ src, name }: { src: string | null | undefined; name: string }) {
  return src ? <Image src={src} alt="" width={40} height={40} unoptimized className="h-10 w-10 rounded-full object-cover" /> : <div className="flex h-10 w-10 shrink-0 items-center justify-center rounded-full bg-emerald-100 text-sm font-bold text-emerald-700">{name.slice(0, 1).toUpperCase()}</div>;
}

function PostCard({ post, onReact, onReport, onComment }: { post: CommunityPost; onReact: (type: CommunityReactionType) => void; onReport: () => void; onComment: (comment: CommunityComment) => void }) {
  const [comments, setComments] = useState<CommunityComment[]>([]);
  const [commentText, setCommentText] = useState('');
  const [loadingComments, setLoadingComments] = useState(false);
  const loadComments = async () => { setLoadingComments(true); try { const response = await communitiesApi.getComments(post.communityId, post.id, { limit: 3 }); setComments(response.data ?? []); } catch { toast.error('Không thể tải bình luận.'); } finally { setLoadingComments(false); } };
  const submitComment = async () => { const body = commentText.trim(); if (!body) return; const response = await communitiesApi.createComment(post.communityId, post.id, { body }); setComments((current) => [...current, response.data]); onComment(response.data); setCommentText(''); };
  return <article className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center gap-3"><Avatar src={post.author.avatarUrl} name={post.author.fullName} /><div className="min-w-0"><p className="truncate text-sm font-bold text-slate-900">{post.author.fullName}</p><p className="text-xs text-slate-400">{new Date(post.createdAt).toLocaleDateString('vi-VN')}{post.status === 'PENDING' ? ' · Đang chờ duyệt' : ''}</p></div><button type="button" onClick={onReport} aria-label="Báo cáo bài viết" className="ml-auto text-slate-400 hover:text-rose-600"><Flag className="h-4 w-4" /></button></div>{post.status === 'PENDING' && <div className="mt-3 rounded-lg bg-amber-50 px-3 py-2 text-xs font-semibold text-amber-700">Bài viết đang chờ ban quản trị duyệt.</div>}{post.content && <p className="mt-4 whitespace-pre-wrap text-sm leading-6 text-slate-700">{post.content}</p>}{(post.topics ?? []).length > 0 && <div className="mt-3 flex flex-wrap gap-2">{post.topics?.map((topic) => <span key={topic} className="rounded-full bg-emerald-50 px-2.5 py-1 text-xs font-semibold text-emerald-700">#{topic}</span>)}</div>}{post.imageUrls.length > 0 && <div className="mt-4 grid grid-cols-2 gap-2">{post.imageUrls.map((url) => <Image key={url} src={url} alt="Ảnh bài viết" width={640} height={360} unoptimized className="aspect-video w-full rounded-lg object-cover" />)}</div>}<div className="mt-4 flex items-center gap-3 border-t border-slate-100 pt-3 text-xs font-semibold text-slate-500"><button type="button" onClick={() => onReact('CHEER')} className={cn('inline-flex items-center gap-1', post.viewerReaction === 'CHEER' ? 'text-rose-600' : 'hover:text-rose-600')}><Heart className="h-4 w-4" fill={post.viewerReaction === 'CHEER' ? 'currentColor' : 'none'} />{post.reactionCount}</button><button type="button" onClick={() => { void loadComments(); }} className="inline-flex items-center gap-1 hover:text-emerald-600"><MessageCircle className="h-4 w-4" />{post.commentCount}</button></div>{(loadingComments || comments.length > 0) && <div className="mt-3 space-y-2">{loadingComments ? <Loader2 className="h-4 w-4 animate-spin" /> : comments.map((comment) => <div key={comment.id} className="rounded-lg bg-slate-50 px-3 py-2 text-xs"><span className="font-bold text-slate-700">{comment.author.fullName}</span><p className="mt-1 text-slate-600">{comment.body}</p></div>)}</div>}<div className="mt-3 flex gap-2"><input value={commentText} onChange={(event) => setCommentText(event.target.value)} onKeyDown={(event) => { if (event.key === 'Enter') void submitComment(); }} placeholder="Viết bình luận..." className="min-w-0 flex-1 rounded-lg border border-slate-200 px-3 py-2 text-xs" /><button type="button" onClick={() => void submitComment()} className="rounded-lg bg-emerald-600 px-3 py-2 text-xs font-bold text-white">Gửi</button></div></article>;
}

export default function CommunityFeed({ communityId }: { communityId: string }) {
  const [posts, setPosts] = useState<CommunityPost[]>([]);
  const [nextCursor, setNextCursor] = useState<string | null>(null);
  const [hasMore, setHasMore] = useState(false);
  const [isLoading, setIsLoading] = useState(true);
  const [isLoadingMore, setIsLoadingMore] = useState(false);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);
  const [selectedFiles, setSelectedFiles] = useState<File[]>([]);
  const [previewUrls, setPreviewUrls] = useState<string[]>([]);
  const fileInputRef = useRef<HTMLInputElement>(null);
  const { register, handleSubmit, reset, formState: { errors, isSubmitting } } = useForm<PostFormValues>({ resolver: zodResolver(postSchema), defaultValues: { content: '', topics: '' } });
  const loadPosts = useCallback(async (cursor?: string) => { if (cursor) setIsLoadingMore(true); else setIsLoading(true); try { const response = await communitiesApi.getPosts(communityId, { cursor, limit: 10, sort: 'LATEST' }); const page = response.data; setPosts((current) => { const merged = cursor ? [...current, ...page.items] : page.items; return Array.from(new Map(merged.map((item) => [item.id, item])).values()); }); setNextCursor(page.nextCursor); setHasMore(page.hasMore); setErrorMessage(null); } catch (error: unknown) { if (!cursor) setErrorMessage(getErrorMessage(error, 'Feed chưa sẵn sàng.')); } finally { setIsLoading(false); setIsLoadingMore(false); } }, [communityId]);
  useEffect(() => { const timer = window.setTimeout(() => { void loadPosts(); }, 0); return () => window.clearTimeout(timer); }, [loadPosts]);
  useEffect(() => () => previewUrls.forEach((url) => URL.revokeObjectURL(url)), [previewUrls]);
  const onFilesSelected = (files: FileList | null) => { const next = Array.from(files ?? []).filter((file) => file.type.startsWith('image/')).slice(0, 10); setSelectedFiles(next); setPreviewUrls(next.map((file) => URL.createObjectURL(file))); };
  const onSubmit = async (values: PostFormValues) => { try { const imageUrls = await Promise.all(selectedFiles.map((file) => uploadApi.uploadImage(file).then((result) => result.url))); const topics = (values.topics ?? '').split(/[\s,]+/).map((topic) => topic.replace(/^#/, '').trim()).filter(Boolean).slice(0, 5); const response = await communitiesApi.createPost(communityId, { content: values.content ?? '', imageUrls, topics }, crypto.randomUUID()); setPosts((current) => [response.data, ...current.filter((post) => post.id !== response.data.id)]); reset(); setSelectedFiles([]); setPreviewUrls([]); toast.success(response.data.status === 'PENDING' ? 'Bài viết đang chờ duyệt.' : 'Đã đăng lên câu lạc bộ.'); } catch (error: unknown) { toast.error(getErrorMessage(error, 'Không thể đăng bài lúc này.')); } };
  return <><section className="space-y-5"><form onSubmit={handleSubmit(onSubmit)} className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center gap-2 text-sm font-bold text-slate-800"><Sparkles className="h-4 w-4 text-emerald-600" />Chia sẻ cùng câu lạc bộ</div><textarea {...register('content')} placeholder="Bạn đang nghĩ gì về trận đấu hôm nay?" className="mt-4 min-h-28 w-full resize-y rounded-lg border border-slate-200 bg-slate-50 p-3 text-sm outline-none transition focus:border-emerald-400 focus:ring-2 focus:ring-emerald-100" disabled={isSubmitting} />{errors.content && <p className="mt-1 text-xs text-rose-600">{errors.content.message}</p>}<input {...register('topics')} placeholder="#pickleball #giaoluu" className="mt-3 w-full rounded-lg border border-slate-200 px-3 py-2 text-xs outline-none focus:border-emerald-400" disabled={isSubmitting} />{previewUrls.length > 0 && <div className="mt-3 flex flex-wrap gap-2">{previewUrls.map((url, index) => <div key={url} className="relative"><Image src={url} alt="Ảnh xem trước" width={96} height={72} unoptimized className="h-18 w-24 rounded-md object-cover" /><button type="button" onClick={() => { setSelectedFiles((current) => current.filter((_, itemIndex) => itemIndex !== index)); setPreviewUrls((current) => current.filter((_, itemIndex) => itemIndex !== index)); }} className="absolute right-1 top-1 rounded-full bg-black/60 p-1 text-white"><X className="h-3 w-3" /></button></div>)}</div>}<input ref={fileInputRef} type="file" accept="image/*" multiple hidden onChange={(event) => onFilesSelected(event.target.files)} /><div className="mt-3 flex items-center justify-between gap-3"><button type="button" onClick={() => fileInputRef.current?.click()} className="flex items-center gap-2 text-xs font-semibold text-slate-500 hover:text-emerald-600"><ImagePlus className="h-4 w-4" />Thêm ảnh</button><button type="submit" disabled={isSubmitting} className="inline-flex items-center gap-2 rounded-lg bg-emerald-600 px-4 py-2 text-xs font-bold text-white transition hover:bg-emerald-700 disabled:cursor-not-allowed disabled:opacity-60">{isSubmitting ? <Loader2 className="h-4 w-4 animate-spin" /> : <Send className="h-4 w-4" />}Đăng bài</button></div></form>{isLoading ? <div className="space-y-4"><div className="h-52 animate-pulse rounded-lg bg-slate-100" /><div className="h-52 animate-pulse rounded-lg bg-slate-100" /></div> : errorMessage ? <div className="rounded-lg border border-slate-200 bg-white p-8 text-center"><p className="text-sm font-semibold text-slate-700">{errorMessage}</p><button type="button" onClick={() => void loadPosts()} className="mt-3 inline-flex items-center gap-2 text-xs font-bold text-emerald-600"><RefreshCw className="h-3.5 w-3.5" />Thử lại</button></div> : posts.length === 0 ? <div className="rounded-lg border border-dashed border-slate-300 bg-white p-12 text-center"><MessageCircle className="mx-auto h-10 w-10 text-slate-300" /><p className="mt-3 text-sm font-semibold text-slate-700">Chưa có bài viết nào</p></div> : <>{posts.map((post) => <PostCard key={post.id} post={post} onReact={(type) => { void communitiesApi.reactToPost(communityId, post.id, type).then((result) => { setPosts((current) => current.map((item) => item.id === post.id ? { ...item, viewerReaction: result.data.reactionType, reactionCount: result.data.count } : item)); }); }} onReport={() => { void communitiesApi.reportPost(communityId, post.id, { reason: 'OTHER' }).then(() => toast.success('Đã gửi báo cáo.')).catch((error: unknown) => toast.error(getErrorMessage(error, 'Không thể báo cáo.'))); }} onComment={() => setPosts((current) => current.map((item) => item.id === post.id ? { ...item, commentCount: item.commentCount + 1 } : item))} />)}{hasMore && <button type="button" disabled={isLoadingMore} onClick={() => void loadPosts(nextCursor ?? undefined)} className={cn('mx-auto flex items-center gap-2 rounded-lg border border-slate-200 bg-white px-4 py-2 text-xs font-bold text-slate-600 hover:border-emerald-300 hover:text-emerald-700', isLoadingMore && 'opacity-60')}>{isLoadingMore && <Loader2 className="h-4 w-4 animate-spin" />}Xem thêm bài viết</button>}</>}</section><ClubChatLauncher communityId={communityId} /></>;
}
